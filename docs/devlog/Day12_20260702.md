- 자본변동표에서 당기순이익을 포착하고 영업이익 뒤에 (손실) 등이 붙은 경우 값을 가져오지 못하는 문제. 계정명 표준화 함수를 만들고 손익계산서에서만 손익 값을 가져오도록 수정. 중복 방지 장치도 추가.

- 사업보고서 분석 시 request too large 에러 발생. 확인 결과 표가 1400개 이상 포함되어 있음. 구체적 수치는 다른 재무정보 fetch tool을 활용하고 사업보고서는 표 파싱을 생략하기로. 