**Day7**
- Tasks: dart_server.py 완성 / 멀티에이전트 Swagger UI 테스트

- Details: 
    정정공시를 쉽고 정확하게 파악할 수 있으면 좋을 것 같아 수정사항을 difflib 라이브러리로 전부 찾아내는 것이 최초의 설계였으나 정정공시들을 여럿 직접 확인해보니 대부분 간단한 정정 전후의 값을 표로 나타내고 있어 BeautifulSoup을 활용해 표를 행 단위 구조 데이터로 추출한 다음 LLM 이 분석하게끔 하는 편이 효율적이라고 판단함. 

    Swagger UI 테스트에서는 간단한 질문들로 테스트하던 중 과거 cp949 인코딩으로 저장된 사업보고서의 경우 utf-8로 하드코딩되어있는 현재의 코드로는 열 수 없다는 사실을 확인하고 fallback 함수를 추가함. 

    또한 trend agent 를 호출하여 활용할 것을 기대한 추세 테스트 쿼리에서 Request too large 에러가 발생하여 business_report_agent가 수개년의 사업보고서를 불러오고 파싱하여 llm 컨텍스트에 넣었다고 추론함. Supervisor의 worker 할당이 원활히 이루어지지 않는다고 판단하여 supervisor prompt에 세부 작업 지시사항을 추가하고 해당 오류에 대한 금지 문구를 작성함.

    동종업종 간 비교 쿼리에서 삼성전자와 같이 다양한 사업을 영위하는 기업의 경우에는 사용자가 의도한 업종분류와 KRX 업종분류 간 불일치가 발생할 수 있음을 테스트 도중 확인함. 사용자가 이 점을 인지하고 "삼성전자와 같은 반도체 업종 회사들 2024년 매출 비교해줘" 라고 쿼리를 작성해도 이미 등록된 업종분류에 따라 답변을 생성하도록 되어있는 agent는 우선 '통신 및 방송 장비 제조업'에 속한 회사들과의 비교를 제공하고 추가로 반도체 업종 제공이 필요한지 반문함. 이는 매우 비효율적이므로 fetch_peers 툴에 industry_override 파라미터를 추가하고 이를 우선적으로 반영하게끔 수정함.